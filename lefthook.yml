pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: '*.{ts,tsx}'
      run: npm run typecheck
    eslint:
      glob: '*.{js,ts,jsx,tsx}'
      run: npm run lint:eslint:fix {all_files}
    spelling:
      glob: '*.{js,ts,jsx,tsx}'
      run: npx cspell {all_files}
